# Prompt used for Solution Architect Agent work.

For this session you are acting as a Solution Architect working out of the C:\Users\ranji\Desktop\Claude Workspace\AIDLC folder. All the files that guide your behavior are in that folder and sub folders starting with the CLAUDE.md, FRAMEWORK.md and CAPABILITIES.md in the root and then the .claude subfolder which follows the standard Claude Code guidelines for artifacts in that subfolder.

Your target project folder is however here: C:\Users\ranji\Desktop\Claude Workspace\tdm-website-nextjs-strapi-modernize

The A04-2-SOLUTION-ARCHITECTURE subfolder there (an `adr/` subfolder already exists for Architecture Decision Records) is where you will place all outputs from this session.

For this project we are migrating a marketing website. For reference the original site is at this location: C:\Users\ranji\Desktop\Claude Workspace\TDWebsite, but you should not need it. All the requirements for the application are in the A01-2-REQUIREMENTS folder in the target project folder. These requirements were generated by an associated Analyst Agent.

For the solution architecture, the target platform is Next.js 14 (App Router) for the front end and Strapi v5 with PostgreSQL as a headless CMS, deployed as an npm-workspaces monorepo (a Next.js app, a Strapi app, and any shared packages needed for typed data access and content migration/seeding). The target directory where the generated source code will be placed is: C:\Users\ranji\Desktop\Claude Workspace\TDWebsite2.

Please review the requirements in the A01-2-REQUIREMENTS folder and, following your Solution Architect guidelines, create a comprehensive set of Solution Architecture documents in one or more .md files. Include descriptive diagrams (mermaid: C4 context/container, ER diagram, sequence diagrams) as appropriate.

The Developer Agent will use these Solution Architecture documents together with the Requirements documents to create the final solution. The QA Architect Agent will also use these Solution Architecture documents together with the Requirements documents to create the comprehensive test plan.

Above all else the solution must be in the form of a set of well-defined functional components (pages/routes, API routes, CMS content types, shared data-access client) with clean, well-defined interfaces for parameter and data exchange. Define the content model (CMS content types and any reusable components/fields) needed to cover every Story in the Requirements. Define how the front end fetches content from the CMS (a typed REST or GraphQL client), how content changes propagate to a statically generated site (e.g. rendering strategy and cache invalidation/revalidation approach), and how any lead-capture/contact form is handled server-side.

The Solution Architecture should include a detailed ER data model of the content types and their relationships.

The Solution Architecture should also include a detailed starting ontology that describes the concepts and relationships between them for the data and objects that will be covered by the solution.

Record the key architectural decisions as ADRs under `A04-2-SOLUTION-ARCHITECTURE/adr/` (e.g. choice of CMS, rendering/caching strategy, monorepo layout, hosting/deployment approach, styling migration strategy).

Ask clarifying questions as needed.

Provide evidence of complete requirements coverage by creating a separate "requirements coverage" document that gives a mapping from Requirements Stories to the component(s) and/or process(es) that will implement each Requirement Story.
